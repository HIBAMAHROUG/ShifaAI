This folder contains files moved from the main tree during a cleanup operation.

Do not delete these files until you are certain they are not needed.

Contents:
- `from reportlab.lib.py` — misplaced Python ReportLab script found in `frontend/js`.
- `app_old.py` — older backend copy moved from `backend/`.
- `desktop.ini` — Windows system file.

If you want any file restored to its original location, move it out of this folder and commit.
